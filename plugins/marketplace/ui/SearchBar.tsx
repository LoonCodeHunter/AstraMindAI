export function SearchBar() {
  return (
    <input
      type="text"
      placeholder="Search plugins..."
      style={{
        padding: "0.5rem",
        width: "300px",
        borderRadius: "6px",
        border: "1px solid #aaa"
      }}
    />
  );
}
