# Prisma Schema

This directory contains the Prisma schema and SQL migrations used by AstraMindAI.

- `schema.prisma` defines the data model.
- `migrations/` contains versioned SQL migrations.
- `seed/` contains initial seed scripts.
