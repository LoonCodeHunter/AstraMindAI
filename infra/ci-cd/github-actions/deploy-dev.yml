name: Deploy Dev

on:
  push:
    branches: [ dev ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Dev
        run: helm upgrade --install astramind-dev infra/helm/astra-mind-ai -f infra/helm/astra-mind-ai/values.dev.yaml
