# Models Registry

The models registry tracks:

- Model versions
- Metadata
- Performance metrics
- Safety evaluations

Models are stored under `/models/registry` and referenced by the ML service and backend.
