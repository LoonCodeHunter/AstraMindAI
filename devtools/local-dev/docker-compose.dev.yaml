version: "3.9"
services:
  backend:
    build: ../../apps/backend
    ports:
      - "4000:4000"
  frontend:
    build: ../../apps/frontend
    ports:
      - "3000:3000"
  ml-service:
    build: ../../apps/ml-service
    ports:
      - "8000:8000"
  worker:
    build: ../../apps/worker
