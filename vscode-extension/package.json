{
  "name": "astramind-vscode",
  "displayName": "AstraMindAI",
  "description": "AI assistant integrated into VS Code.",
  "version": "1.0.0",
  "publisher": "LoonCodeHunter",
  "private": false,

  "engines": {
    "vscode": "^1.1.37"
  },

  "categories": [
    "AI",
    "Productivity",
    "Development",
    "Machine Learning"
  ],

  "activationEvents": [
    "onCommand:astramind.chat",
    "onCommand:astramind.explainCode",
    "onCommand:astramind.generateDocs",
    "onCommand:astramind.refactor",
    "onCommand:astramind.switchModel",
    "onView:astramind.chatView"
  ],

  "main": "./dist/extension.js",

  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "astramind",
          "title": "AstraMind",
          "icon": "src/assets/icon.svg"
        }
      ]
    },
    "views": {
      "astramind": [
        {
          "id": "astramind.chatView",
          "name": "AstraMind Chat"
        }
      ]
    },
    "commands": [
      { "command": "astramind.chat", "title": "AstraMind: Chat" },
      { "command": "astramind.explainCode", "title": "AstraMind: Explain Code" },
      { "command": "astramind.generateDocs", "title": "AstraMind: Generate Docs" },
      { "command": "astramind.refactor", "title": "AstraMind: Refactor Code" },
      { "command": "astramind.switchModel", "title": "AstraMind: Switch Model" }
    ]
  },

  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "typecheck": "tsc --noEmit",

    "package": "pnpm bundle && pnpm vsce package --no-dependencies",
    "publish": "pnpm bundle && pnpm vsce publish --no-dependencies"
  },

  "dependencies": {
    "axios": "^1.7.7"
  },

  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.1.37",
    "@typescript-eslint/eslint-plugin": "^8.10.0",
    "@typescript-eslint/parser": "^8.10.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0",
    "vsce": "^2.15.0"
  }
}
